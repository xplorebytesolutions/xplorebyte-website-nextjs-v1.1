NEXT_PUBLIC_GA_ID=

# Postgres connection (server-side only)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME

# Set to "true" if your provider requires SSL (common on managed Postgres)
DATABASE_SSL=false
DATABASE_SSL_REJECT_UNAUTHORIZED=false

# Optional: set to hash IPs for spam/rate-limiting without storing raw IP
LEADS_IP_SALT=
